<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">

	<!-- 게시판 글 작성 -->
	<insert id="insert">
		INSERT INTO BOARD( BOARD_NUM, MEMBER_NUM, PLACE, CONTENT, REGDATE, TRIPDATE, td_like_cnt )
		VALUES( BOARD_SEQ.NEXTVAL, #{memberNum} , #{place}, #{content}, SYSDATE, #{tripdate}, 0)

	</insert>
	
	<select id="list" resultType="com.tripdiary.vo.BoardVo">
		SELECT   BOARD_NUM
			   , REGDATE
			   , TRIPDATE
			   , td_like_cnt
		  FROM MP_BOARD
		  ORDER BY BNO DESC
	</select>


</mapper>